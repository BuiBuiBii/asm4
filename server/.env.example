# Local or Render
PORT=5000
NODE_ENV=development
MONGO_URI=mongodb://127.0.0.1:27017/SimpleQuiz
JWT_SECRET=replace-with-a-long-random-secret
# Comma-separated origins, no trailing slash needed.
CORS_ORIGIN=http://localhost:5173
